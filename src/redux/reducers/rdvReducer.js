/*export const setRdv = (rdv) => ({
    type: 'SET_RDV',
    payload: rdv,
});


const initialState = {
    description: '',
    typeRdv: '',
    jourDebut: '',
    jourFin: '',
    heureDebut: '',
    heureFin: '',
    minuteDebut: '',
    minuteFin: '',
    dureeRdv: '',
    jourDisponible: '',
};

const rdvReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'SET_RDV':
            return {
                ...state,
                rdv: action.payload,
            };
        default:
            return state;
    }
};

export default rdvReducer;*/